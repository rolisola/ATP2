#include <stdio.h>

/*
    Entrada:
        A primeira linha de entrada contêm um inteiro positivo N indicando a quantidade de elementos do vetor. A segunda linha de entrada possui N elementos Ei, separados por espaço, indicando os elementos que compõem o vetor.

    Saída:
        Faça uma função recursiva que receba um vetor e a quantidade de elementos do vetor e retorne o maior elemento desse vetor. Por fim, na main, imprima este elemento.

    Restrições:
        0 ≤ N ≤ 1000.
        -10000 ≤ Ei ≤ 10000.
    
    Exemplos:
        Entrada	    Saída
           5
     20 8 -8 38 10	 38

    Média Ponderada:
        Considere que este exercício tem peso 1 (um) na média ponderada das notas dos exercícios.
*/
int procurar();
int main()
{
    
    return 0;
}
